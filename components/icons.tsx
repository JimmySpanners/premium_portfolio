import { LucideProps, Loader2 } from "lucide-react"

export const Icons = {
  spinner: Loader2,
  // Add any custom icons here
  // Example:
  // logo: (props: LucideProps) => <span {...props}>📸</span>,
}
